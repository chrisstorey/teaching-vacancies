package com.example.twelvefactorapp.model.enums;

public enum WorkingPattern {
    FULL_TIME,
    PART_TIME,
    JOB_SHARE,
    COMPRESSED_HOURS,
    STAGGERED_HOURS
    // Add other working patterns as defined in the Rails app
}
